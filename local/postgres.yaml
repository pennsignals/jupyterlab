!postgres
database: ${POSTGRES_DATABASE}
host: ${POSTGRES_HOST}
password: ${POSTGRES_PASSWORD}
port: ${POSTGRES_PORT}
schema: test
sql: !asset
  path: /tmp/sql/postgres
  ext: .sql
username: ${POSTGRES_USERNAME}
